pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp style.css public/
    - cp script.js public/ || true
    - cp CV.pdf public/ || true
  artifacts:
    paths:
      - public
  only:
    - main